// ================================
// 🔁 MOTOR DE VOZ PASIVA
// ================================

import { AppState } from "./core.state.js";

export function generatePassive(row) {
  // código actual generatePassive()
}

export function buildPassiveEN(subject, verb) {
  // BE + V3
}

export function buildPassiveES(subject, verb) {
  // ser + participio
}
